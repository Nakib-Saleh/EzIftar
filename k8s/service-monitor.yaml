apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: eziftar-services
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      monitor: "true"
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
